#######################File Description#######################


1. gendoc.sh : script generate javascipt SDK document(require jsdoc and docstrap installed);
                (Note: by default, this script will generate a folder 'webrtc-jsdoc-cosmo' for:
                    +conference.js
                    +stream.js
                    +events.js
                    +N.API.js
                    +ui.js
                If extra javascript document is needed, just pass the path.
                For example: ./gendoc.sh /my/path/to/peer.js
                this will generate document with above source code files as well as peer.js)
2. jsdoc.conf.json : configuation of jsdoc and docstrap;
3. template/ : docstrap template package; template/README.md: markdown format index page of WEBRTC javascript SDK;
4. JornalProcess.class : post-process code for cosmo template;
5. JSDocformat : comment template for jsdoc. 
6. LICENSE: license for Jsdoc and DocStrap.
